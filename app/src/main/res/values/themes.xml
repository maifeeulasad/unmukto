<resources>

    <!--
        No android:statusBarColor. From API 35 the system draws the bars itself and
        ignores that attribute, so setting it described a dark bar that never appeared
        while the icons stayed light — white on white. MainActivity sets the icon
        appearance from the current theme instead.
    -->
    <style name="Theme.Unmukto" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryVariant">@color/brand_primary_dark</item>
        <item name="colorOnPrimary">@color/brand_on_primary</item>
        <item name="colorSecondary">@color/brand_accent</item>
        <item name="colorSecondaryVariant">@color/brand_accent</item>
        <item name="colorOnSecondary">@color/white</item>
    </style>

</resources>
